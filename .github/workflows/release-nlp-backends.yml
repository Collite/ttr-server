name: Release NLP Backends (GHCR)

# Build + push the four nlp ENGINE BACKEND images — MorphoDiTa, NameTag 3, Stanza, spaCy —
# to ghcr.io/collite/nlp-<backend>:<version>.
#
# WHY A SEPARATE, MANUAL-ONLY WORKFLOW (not release-image.yml):
#   - Each backend is its own image built from services/nlp/backends/<backend>/ with a
#     self-contained context (its own baked-model stage); they are not in the module map.
#   - The UFAL backends (morphodita, nametag3) bake CC BY-NC-SA models — *publishing* the images
#     is a gated legal item (FI-4, see services/nlp/README.md). So there is NO push trigger:
#     every publish is a deliberate human `workflow_dispatch`. spaCy/Stanza carry permissive models.
#
# MODEL SOURCE (UFAL backends only) — set these as repo Actions VARIABLES
# (Settings → Secrets and variables → Actions → Variables; they are public LINDAT URLs, not secrets):
#   NLP_MORPHODITA_MODEL_URL   (+ optional NLP_MORPHODITA_MODEL_SHA256 to digest-pin the download)
#   NLP_NAMETAG3_MODEL_URL     (+ optional NLP_NAMETAG3_MODEL_SHA256)
# spaCy/Stanza need no model arg (models are baked from pip in their Dockerfiles).
#
# Auth: the TATRMAN_PKG_TOKEN PAT, with GITHUB_TOKEN as the fallback — the same pair
# release-image.yml uses, and for the same reason.
#
# ⛔ GITHUB_TOKEN ALONE IS NOT ENOUGH HERE, and no amount of retrying changes that. GHCR grants a
# repository's GITHUB_TOKEN write on a package only when that package was CREATED by a run in that
# repository. `nlp-morphodita` and `nlp-nametag3` were not: the P(-1) `ttr-` prefix drop put them in
# place by COPYING the `ttr-nlp-*` originals, so they are linked to no repository at all and the
# token gets `denied: permission_denied: The requested installation does not exist` forever. Every
# push this workflow ever made went to the `ttr-`-prefixed names, which is why it never hit this.
#
# The PAT authenticates as a USER, so per-package repository access does not apply and every package
# the owner can write is reachable. GITHUB_TOKEN stays the fallback so the workflow still runs in a
# fork or with no secrets set.

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (no leading v), e.g. 0.9.0'
        required: true
        default: '0.9.0'
      backends:
        description: '"all" or a space-separated subset: morphodita nametag3 stanza spacy'
        required: true
        default: 'all'

env:
  REGISTRY: ghcr.io
  IMAGE_ORG: collite

jobs:
  plan:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.plan.outputs.matrix }}
    steps:
      - id: plan
        shell: bash
        run: |
          set -euo pipefail
          ALL="morphodita nametag3 stanza spacy"
          REQ="${{ github.event.inputs.backends }}"
          [ "$REQ" = "all" ] && REQ="$ALL"
          items=""
          for b in $REQ; do
            case " $ALL " in
              *" $b "*) : ;;
              *) echo "::error::unknown backend '$b' (valid: $ALL)"; exit 1 ;;
            esac
            items="${items}{\"backend\":\"$b\"},"
          done
          echo "matrix={\"include\":[${items%,}]}" >> "$GITHUB_OUTPUT"

  build:
    needs: plan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.plan.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v5

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ secrets.TATRMAN_PKG_USER != '' && secrets.TATRMAN_PKG_USER || github.actor }}
          password: ${{ secrets.TATRMAN_PKG_TOKEN != '' && secrets.TATRMAN_PKG_TOKEN || secrets.GITHUB_TOKEN }}

      - id: cfg
        shell: bash
        env:
          MORPHODITA_URL: ${{ vars.NLP_MORPHODITA_MODEL_URL }}
          MORPHODITA_SHA: ${{ vars.NLP_MORPHODITA_MODEL_SHA256 }}
          NAMETAG3_URL: ${{ vars.NLP_NAMETAG3_MODEL_URL }}
          NAMETAG3_SHA: ${{ vars.NLP_NAMETAG3_MODEL_SHA256 }}
        run: |
          set -euo pipefail
          B="${{ matrix.backend }}"
          echo "context=services/nlp/backends/$B" >> "$GITHUB_OUTPUT"
          echo "image=${REGISTRY}/${IMAGE_ORG}/nlp-$B" >> "$GITHUB_OUTPUT"

          # UFAL backends require MODEL_URL; MODEL_SHA256 is optional (digest-pin). Only emit an arg
          # when set — passing an empty MODEL_SHA256 would trip the Dockerfile's checksum guard.
          url=""; sha=""
          case "$B" in
            morphodita) url="$MORPHODITA_URL"; sha="$MORPHODITA_SHA" ;;
            nametag3)   url="$NAMETAG3_URL";   sha="$NAMETAG3_SHA" ;;
          esac
          if { [ "$B" = "morphodita" ] || [ "$B" = "nametag3" ]; } && [ -z "$url" ]; then
            echo "::error::$B needs its model URL — set repo Actions variable NLP_$(echo "$B" | tr a-z A-Z)_MODEL_URL"
            exit 1
          fi
          {
            echo "build_args<<__EOF__"
            [ -n "$url" ] && echo "MODEL_URL=$url"
            [ -n "$sha" ] && echo "MODEL_SHA256=$sha"
            echo "__EOF__"
          } >> "$GITHUB_OUTPUT"

      - uses: docker/build-push-action@v6
        with:
          context: ${{ steps.cfg.outputs.context }}
          platforms: linux/amd64
          push: true
          provenance: false          # plain single-arch manifest, matching the other server images
          tags: ${{ steps.cfg.outputs.image }}:${{ github.event.inputs.version }}
          build-args: ${{ steps.cfg.outputs.build_args }}
